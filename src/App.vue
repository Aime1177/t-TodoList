<script setup>
import {ref,onMounted,computed,watch } from 'vue'

const todos = ref([])
const name = ref('')

const inputContent = ref('')
const inputCategories = ref(null)

</script>

<template>
  <header>
   <h1 class="font-bold text-3xl">Hello World</h1>
  </header>

  
</template>

